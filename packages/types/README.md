# packages/types

Shared type definitions. Pydantic schemas on the API side and their TS
counterparts on the web side are generated from here so the two cannot drift
(v2 section 14).

**Owned by Phase 9** (WebSocket infra), where the first cross-boundary contract
appears.
