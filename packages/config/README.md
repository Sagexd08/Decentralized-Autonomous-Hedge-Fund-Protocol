# packages/config

Shared configuration: allocation parameters, risk limits, reputation weights.
Every weight is configurable, never a hardcoded magic number (v2 section 9).

**Owned by Phase 6** (reputation engine).
